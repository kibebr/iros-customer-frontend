import styled from 'styled-components/native'
import { tw } from '@app/tailwind'

export const DefaultView = styled.View(tw('bg-gray-100 flex-1'))
export const ViewTitle = styled.Text(tw('font-bold text-xl my-2'))
